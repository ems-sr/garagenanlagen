[
  {
    "id": "alterNullability.dropNotNull.invoice.previousReadingId",
    "label": "Drop NOT NULL on \"invoice\".\"previousReadingId\"",
    "operationClass": "widening",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "previousReadingId",
        "table": "invoice"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"previousReadingId\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "invoice",
          "previousReadingId"
        ]
      }
    ],
    "execute": [
      {
        "description": "drop NOT NULL on \"previousReadingId\"",
        "sql": "ALTER TABLE \"public\".\"invoice\" ALTER COLUMN \"previousReadingId\" DROP NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"previousReadingId\" is nullable",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3 AND \"is_nullable\" = $4)) AS \"result\"",
        "params": [
          "public",
          "invoice",
          "previousReadingId",
          "YES"
        ]
      }
    ]
  }
]